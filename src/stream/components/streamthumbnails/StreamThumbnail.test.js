import React from 'react'
import renderer from 'react-test-renderer';
import StreamThumbnail from './StreamThumbnail';
import Provider from 'react-redux'
import store from '../../../store'

it('renders a thumbnail with data', () => {
  
})
